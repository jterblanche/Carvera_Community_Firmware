# Vendored heap_5

`heap_5.c` is vendored from FreeRTOS-Kernel V11.3.0, commit `9b777ae`:

`portable/MemMang/heap_5.c`

Upstream repository: <https://github.com/FreeRTOS/FreeRTOS-Kernel>

Local changes are limited to the include block marked
`CARVERA HEAP PORT BEGIN/END`; the project compatibility types and hooks live in
`heap_port.h`.
